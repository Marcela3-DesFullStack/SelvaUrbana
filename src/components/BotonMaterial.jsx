import React from 'react';
import { Button, Typography } from '@mui/material';

const MyComponent = () => {
  return (
    <div>
      <Typography variant="h1">Hola, Mundo!</Typography>
      <Button variant="contained" color="primary">Agregar Planta</Button>
    </div>
  );
};

export default MyComponent;
